---
title: 'Painéis das Unidades'
format: 
  html:
    embed-resources: true

---

```{r echo=FALSE}
endereco <-
  '../dados/IndicadoresPainelUnidade.rds'

indicadores <- readr::read_rds(endereco)

selecao <- indicadores |> 
  dplyr::filter(!is.na(Unidade)) |> 
  dplyr::mutate(
    Unidade = sprintf('<a href="relatorio_%s.html">%s</a>', Id, Unidade)
  ) |> 
  dplyr::select(Unidade)


kableExtra::kable(
  selecao,
  align = 'c',
  escape = FALSE,
  col.names = NULL
)
```

